

shon, this is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started


tor steps:

1. git clone [https://github.com/uncoolburrito/sutapaebook.git](https://github.com/uncoolburrito/sutapaebook.git)

2. cd sutapaebook (depends on where u download the folder)

3. npm install

4. npm run dev

u can also do:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```
pretty sure tui yarn, bun kichhu use koris na, so just do npm run dev

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result (or actually jokhon npm run korbi they will show http://localhost:3000 press command and click on the link)

You can start editing the page by modifying `app/page.tsx`. The page auto-updates as you edit the file.




## ALSO THIS IS MY SECRET GITHUB SO DO NOT DARE TO SHARE THE GITHUB WITH ANYONE
